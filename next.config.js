/** 
 * ============================================
 * NEXT.JS CONFIGURATION FILE
 * ============================================
 * 
 * This is like a settings file for the Next.js framework.
 * Think of it like Django's settings.py or Flask's config.
 * 
 * For now, it's mostly empty because the defaults work great.
 * You'd add stuff here for things like:
 * - Custom image domains
 * - Redirects
 * - Environment-specific settings
 * ============================================
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental features if needed
  // For now, we're using defaults which work great
}

// Export the config (like module.exports in Python would be)
module.exports = nextConfig
